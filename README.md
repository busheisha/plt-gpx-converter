<<<<<<< HEAD
# plt-gpx-converter
Скрипт, который помогает скачать с Латрекса трек в формате plt, превращает в gpx, и можно скормить его аниматору. Ещё может объединить точки с треком
=======
# 🗺️ Конвертер GPS треков PLT → GPX

Простой инструмент для конвертации файлов треков из формата PLT (OziExplorer) в формат GPX с правильным временем и структурой. Написан преимущественно могущественными силами неестественного интеллекта, за что им большое спасибо.

## 📋 Что умеет программа

- ✅ **Конвертация PLT → GPX** - преобразует треки из формата OziExplorer в стандартный GPX
- ✅ **Автоматическое исправление кодировки** - работает с русскими символами
- ✅ **Добавление времени** - каждая точка трека получает правильное время
- ✅ **Обработка waypoints** - может добавить треки к файлам с waypoints
- ✅ **Пакетная обработка** - конвертирует все файлы в папке одной командой

## 🚀 Быстрый старт

### 1. Установка зависимостей

Первый раз нужно установить библиотеки:

```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 2. Конвертация файлов

Положите ваши `.plt` файлы в папку `tracks/` и запустите:

```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Конвертировать все PLT файлы в GPX
python main.py convert
```

**Готово!** Ваши `.gpx` файлы появятся в той же папке.

## 📖 Подробные инструкции

### Режимы работы

Программа работает в нескольких режимах:

#### 1️⃣ Конвертация PLT → GPX

```bash
python main.py convert
```

**Что делает:**
- Находит все `.plt` файлы в папке
- Конвертирует каждый в `.gpx`
- Добавляет правильное время к каждой точке
- Создает структуру трека как в OziExplorer

**Пример:**
```
input1.plt → input1.gpx
input2.plt → input2.gpx
input3.plt → input3.gpx
```

#### 2️⃣ Добавление треков к waypoints

```bash
python main.py waypoints
```

**Что делает:**
- Находит `.gpx` файлы с waypoints без треков
- Создает трек из первой и последней точки по времени
- Сохраняет резервную копию с суффиксом `_orig.gpx`

#### 3️⃣ Полная обработка

```bash
python main.py all
```

**Что делает:**
- Сначала конвертирует все PLT → GPX
- Затем обрабатывает waypoints

#### 4️⃣ Интерактивный режим

```bash
python main.py
```

Появится меню:
```
Выберите действие:
1. Конвертировать PLT → GPX
2. Добавить треки к waypoints
3. Сделать все (конвертация + waypoints)
4. Показать справку
```

Просто введите номер нужного действия.

#### 5️⃣ Справка

```bash
python main.py help
```

Показывает все доступные команды.

## 🔧 Структура проекта

```
tracks/
├── main.py                    # Главный файл (запускать его)
├── plt_to_gpx_converter.py    # Модуль конвертации PLT → GPX
├── add_waypoints.py           # Модуль обработки waypoints
├── requirements.txt           # Список зависимостей
├── README.md                  # Эта инструкция
├── venv/                      # Виртуальное окружение Python
├── *.plt                      # Ваши исходные файлы
└── *.gpx                      # Результаты конвертации
```

## 📝 Примеры использования

### Пример 1: Конвертировать один раз

```bash
# 1. Положить файлы input1.plt, input2.plt в папку
# 2. Открыть терминал в папке tracks/
# 3. Запустить:

source venv/bin/activate
python main.py convert

# Готово! Появятся input1.gpx, input2.gpx
```

### Пример 2: Конвертировать новые файлы

```bash
# Положили новые файлы → просто запустить снова:
source venv/bin/activate
python main.py convert
```

### Пример 3: Обработать все

```bash
# Конвертация + обработка waypoints:
source venv/bin/activate
python main.py all
```

## ⚙️ Настройки

### Фиксированная дата

По умолчанию все треки получают дату **15 сентября 2025**.

Чтобы изменить, откройте `plt_to_gpx_converter.py` и найдите строку:

```python
# Фиксированная дата: 15 сентября 2025
fixed_date = datetime(2025, 9, 15)
```

Измените на нужную дату, например:

```python
fixed_date = datetime(2025, 10, 1)  # 1 октября 2025
```

### Отладочная информация

По умолчанию показываются первые 3 точки каждого трека.

Чтобы отключить, откройте `plt_to_gpx_converter.py` и закомментируйте:

```python
# Отладочная информация (можно убрать в продакшене)
if points_processed < 3:
    print(f"Точка {points_processed + 1}: lat={lat}, lon={lon}, time={timestamp}")
```

## 🐛 Решение проблем

### Ошибка: `No module named 'gpxpy'`

**Решение:**
```bash
source venv/bin/activate
pip install -r requirements.txt
```

### Ошибка: `python: command not found`

**Решение:** Используйте `python3` вместо `python`:
```bash
python3 main.py convert
```

### Файлы не конвертируются

**Проверьте:**
1. Файлы имеют расширение `.plt`
2. Вы находитесь в папке `tracks/`
3. Виртуальное окружение активировано

*

### Неправильная кодировка (кракозябры)

Программа автоматически пробует разные кодировки:
- UTF-8
- CP1251 (Windows Cyrillic)
- Latin-1
- ISO-8859-1

Если проблема осталась, откройте `plt_to_gpx_converter.py` и добавьте нужную кодировку в список:

```python
encodings_to_try = ['utf-8', 'cp1251', 'latin-1', 'iso-8859-1', 'ваша_кодировка']
```

## 📚 Технические детали

### Формат PLT (OziExplorer)

Структура файла:
```
Строка 1-6: Заголовок
Строка 7+:  Данные точек (lat, lon, flag, elevation, time_days, date, time)
```

Пример строки:
```
56.281474, 37.375393, 1, 405.7, 45915.3071644, 15-сен-25, 7:22:19
```

### Формат GPX

Создается структура:
```xml
<gpx>
  <metadata>
    <time>...</time>
    <bounds>...</bounds>
  </metadata>
  <trk>
    <name>...</name>
    <trkseg>
      <trkpt lat="..." lon="...">
        <time>...</time>
      </trkpt>
    </trkseg>
  </trk>
</gpx>
```

### Что делает конвертер

1. **Читает PLT файл** с автоопределением кодировки
2. **Парсит координаты** (широта, долгота)
3. **Извлекает время** из столбца 6 (формат `7:22:19`)
4. **Комбинирует** с фиксированной датой (15 сентября 2025)
5. **Создает GPX трек** с правильной структурой
6. **Добавляет метаданные** (bounds, time)
7. **Сохраняет** в формате GPX 1.1

## 🤝 Поддержка

Если что-то не работает:
1. Проверьте, что виртуальное окружение активировано
2. Убедитесь, что все зависимости установлены
3. Проверьте формат входных файлов
4. Посмотрите сообщения об ошибках в терминале

## 📄 Лицензия

Свободное использование для любых целей.

---

**Приятной работы с GPS треками!** 🗺️✨
>>>>>>> a88e358 (Initial commit)
